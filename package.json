{
  "name": "stock-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted autonomous stock/crypto research, tracking & trade-proposal desk. Mirrors the agent-studio pattern, wired to the Robinhood MCP. Autonomous everywhere except the final order placement, which requires a human approval click.",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "agent": "node src/agent/scheduler.js",
    "research": "node src/agent/research.js",
    "tracking": "node src/agent/tracking.js",
    "proposals": "node src/agent/proposals.js",
    "check": "node --check src/server.js && node --check src/agent/scheduler.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
